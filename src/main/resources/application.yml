spring:
  datasource:
    driver-class-name: org.postgresql.Driver

    url: jdbc:postgresql://15.164.24.60:5432/pg_db
    username: db_user
    password: pg_password
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
